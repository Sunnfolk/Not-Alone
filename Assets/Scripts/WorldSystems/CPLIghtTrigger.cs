using UnityEngine;

namespace WorldSystems
{
    public class CPLIghtTrigger : MonoBehaviour
    {
        // Start is called before the first frame update [Among us]

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
